//! A user's standing within one server.

// One-def-per-file: this directory groups membership defs; `membership.rs` holds `Membership` itself.
#[allow(clippy::module_inception)]
pub mod membership;
pub mod tier;
